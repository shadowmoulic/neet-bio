// Topbar and footer are now static-rendered directly into index.html (see
// generate_home_static.js) so crawlers and no-JS visitors get real markup
// instead of an empty <body>. This file intentionally left as a no-op stub
// to avoid duplicating that HTML at runtime.
